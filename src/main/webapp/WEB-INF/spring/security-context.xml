<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
                        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.1.xsd">
    
    <http auto-config="true" use-expressions="true" access-denied-page="/accessDenied.jsp" >
        <form-login login-page="/login.jsp" default-target-url="/" 
                    authentication-failure-url="/login.jsp?error=true" />
        <!--<intercept-url ref="ajaxTimeoutRedirectFilter" after="EXCEPTION_TRANSLATION_FILTER"/>-->
        <intercept-url pattern="/" access="hasAnyRole('ROLE_GUEST','ROLE_ADMIN')" />
        <intercept-url pattern="/users*" access="hasRole('ROLE_ADMIN') and fullyAuthenticated" />
        <intercept-url pattern="/roles*" access="hasRole('ROLE_ADMIN') and fullyAuthenticated" />
    </http>
    
    <authentication-manager>
        <authentication-provider>
            <password-encoder hash="md5" />
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query="SELECT name as username, password as password, 'true' as enabled FROM WEB_USER
                                        where name = ?"
                               authorities-by-username-query="SELECT WEB_USER_name as username, roles_name as authorities FROM WEB_USER_WEB_ROLE
                                        WHERE WEB_USER_name = ?" />
        </authentication-provider>
    </authentication-manager>
    
</beans:beans>